<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.qk.pagescutsample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".PageCutApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,UnusedAttribute">
        <activity
            android:name=".SampleActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".minipage.MiniPageActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".transition.AndroidTransitionFrameworkActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".transition.AndroidTransitionFrameworkActivity2"
            android:screenOrientation="portrait" />
        <activity
            android:name=".fragment.FragmentTransitionActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>